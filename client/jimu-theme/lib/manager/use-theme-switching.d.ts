import { type ThemeModule } from './theme-module';
/**
 * When switching the theme or customizing the theme, generate the latest theme variable and theme module.
 * @internal
 */
export declare const useThemeSwitching: () => ThemeModule;
